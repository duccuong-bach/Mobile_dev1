<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cubesystem.kurimabank">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="cubesystem.kurimabank.model.MyApplication"
        android:label="@string/login"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <activity android:name="cubesystem.kurimabank.activity.Login"
            android:label="@string/login">
            <!--android:label="@string/title_step1_2">-->
            <intent-filter android:label="@string/launcher_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="cubesystem.kurimabank.activity.Step2Activity"
            android:label="@string/register">

        </activity>
        <activity android:name="cubesystem.kurimabank.activity.Step3Activity"
            android:label="@string/summary">

        </activity>


    </application>




</manifest>
